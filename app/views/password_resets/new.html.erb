<% provide(:title, "Forgot password") %>

<div id ="forgot_pass">
<br>
<h2>Forgot password</h2>
<div class="row">
  <div class="containerZ col-md-8 col-md-offset-2">
    <%= form_for(:password_reset, url: password_resets_path) do |f| %>
      <%= f.label :email %>
      <%= f.email_field :email, placeholder: "Enter your email to reset password", class: 'form-control' %>
      <%= f.submit "Submit", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
</div>
